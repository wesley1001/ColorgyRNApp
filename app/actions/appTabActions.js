import { createAction } from 'redux-actions';

export const selectTab = createAction('SELECT_TAB');
export const hideAppTabBar = createAction('HIDE_APP_TAB_BAR');
export const showAppTabBar = createAction('SHOW_APP_TAB_BAR');
